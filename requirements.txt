# QUILL — runtime Python dependencies (production).
# Dev-only deps (pytest, etc.) live in requirements-dev.txt.

# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# DB (async PostgreSQL) — currently unused at runtime; in-memory repository is default.
asyncpg==0.30.0
sqlalchemy==2.0.36

# Config / schema
pyyaml==6.0.2
jsonschema==4.23.0

# Ingestion parsers
pypdf==5.1.0
python-docx==1.1.2
markdown-it-py==3.0.0

# Tier 2 local LLM client. ollama 0.4.4 requires httpx 0.27.x — let pip pick
# the compatible httpx rather than pinning a conflicting version.
ollama==0.4.4
httpx>=0.27.0,<0.28.0

# Auth + signing (QUILL-native; DECISION-002)
PyJWT==2.10.1
python-gnupg==0.5.4
passlib[bcrypt]==1.7.4

# Multipart file uploads in FastAPI (used by /artifacts upload).
python-multipart>=0.0.9
